{
  "name": "learning-planet",
  "private": true,
  "scripts": {
    "build:math-teaching": "vite build --config vite.math-teaching.config.mjs",
    "build:optics": "vite build --config vite.optics.config.mjs",
    "build:earth-lab": "vite build --config vite.earth-lab.config.mjs",
    "dev": "vite",
    "build:earth-orbit": "vite build",
    "typecheck": "tsc --noEmit",
    "test:earth-orbit": "vitest run",
    "build:experiments": "vite build --config vite.experiments.config.ts",
    "build:stories": "vite build --config vite.story.config.ts",
    "check": "npm run typecheck && npm run test:earth-orbit && npm run build:earth-orbit && npm run build:experiments && npm run build:stories && npm run build:little-star && npm run build:math-teaching && npm run build:optics && npm run build:earth-lab",
    "deploy": "npx -y firebase-tools@latest deploy --only hosting",
    "deploy:all": "npx -y firebase-tools@latest deploy",
    "build:little-star": "vite build --config vite.little-star.config.ts"
  },
  "dependencies": {
    "pixi.js": "^8.18.1",
    "rubiks-cube-solver": "^1.2.0",
    "three": "^0.180.0"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "@types/three": "^0.180.0",
    "typescript": "^5.9.2",
    "vite": "^7.1.3",
    "vitest": "^3.2.4"
  }
}
